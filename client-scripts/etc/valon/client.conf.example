# VALON Client Configuration

# WireGuard interface
WG_INTERFACE=wg0

# Discovery Role - Bootstrap connection (REQUIRED for initial setup)
# Public key of Discovery Role
DISCOVERY_PUBKEY=ylVE/jMSSR1E6Dzf0jGwJZ+29z/NWYYayc82LxQqFgQ=

# Fixed endpoint for Discovery Role (before DNS is available)
# Format: IP:PORT
DISCOVERY_ENDPOINT=192.168.10.30:51820

# Allowed IPs for Discovery Role (default: only Discovery's WireGuard IP)
DISCOVERY_ALLOWED_IPS=100.100.0.1/32

# Discovery Role DNS server (after connected)
DISCOVERY_DNS=100.100.0.1

# Discovery Role DDNS API
DISCOVERY_API=http://100.100.0.1:8053

# DNS zone
DNS_ZONE=valon.internal

# Own WireGuard IP (for DDNS registration)
OWN_WG_IP=100.100.0.10

# Own alias (for DDNS registration)
OWN_ALIAS=client01

# Connection test settings
PING_COUNT=2
PING_TIMEOUT=1

# Default WireGuard port
WG_PORT=51820

# Allowed IPs for peers (default: allow all WireGuard subnet)
ALLOWED_IPS=100.100.0.0/24
